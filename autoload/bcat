# depends: bat

bcat() (
  # cat a binary file
  for bin in "$@"; do
    LOCATION=$(whence -p "$bin")
    if [[ -z "$LOCATION" ]]; then
      echo "no such bin: $bin" 1>&2
    else
      bat "$LOCATION"
    fi
  done
)
